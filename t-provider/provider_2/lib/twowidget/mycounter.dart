import 'package:flutter/material.dart';
import 'package:provider_2/model.dart';

class MyCouunterWidget extends StatelessWidget {
  MyCouunterWidget({Key key}):super(key:key);
  @override
  Widget build(BuildContext context) {
    final myModel= context.widget<MyModel>();
    return Container(
      
    );
  }
}